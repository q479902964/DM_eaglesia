{
  "name": "server",
  "version": "1.0.0",
  "description": "an express server using to mock data",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
    "private": true,
  "dependencies": {
    "express": "4.x"
  }
}
